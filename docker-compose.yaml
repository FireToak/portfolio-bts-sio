---
# Local development environment for a PHP web application
services:
  web:
    image: php:8.2-apache
    container_name: portfolio-web-dev
    ports:
      - "80:80"
    volumes:
      - ./:/var/www/html/